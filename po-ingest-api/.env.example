HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Optional: set to an MLflow tracking server URL to trace the convert_pdf_to_markdown
# MCP tool call. Left unset, tracing is disabled outright (no local ./mlruns writes).
MLFLOW_TRACKING_URI=
