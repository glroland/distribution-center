{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "adc.wmsApi.serviceName" . }}
  labels:
    {{- include "adc.commonLabels" . | nindent 4 }}
    app.kubernetes.io/name: wms-api
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: wms-api
spec:
  selector:
    app.kubernetes.io/name: wms-api
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.global.wmsApi.port }}
      targetPort: http
{{- end }}
