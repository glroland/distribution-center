apiVersion: v2
name: wmsApi
description: >-
  In-memory inventory ledger for one virtual distribution center, seeded
  on startup from a CSV ConfigMap.
type: application
version: 0.1.0
appVersion: "0.1.0"
