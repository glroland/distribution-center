{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "adc.shippingApi.serviceName" . }}
  labels:
    {{- include "adc.commonLabels" . | nindent 4 }}
    app.kubernetes.io/name: shipping-api
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: shipping-api
spec:
  selector:
    app.kubernetes.io/name: shipping-api
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.global.shippingApi.port }}
      targetPort: http
{{- end }}
