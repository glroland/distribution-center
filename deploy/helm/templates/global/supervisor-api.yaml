{{- if .Values.supervisorApi.enabled }}
{{- $name := printf "%s-supervisor-api" (include "adc.fullname" .) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
  labels:
    {{- include "adc.commonLabels" . | nindent 4 }}
    app.kubernetes.io/name: supervisor-api
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: supervisor-api
spec:
  replicas: {{ .Values.supervisorApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: supervisor-api
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: supervisor-api
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: supervisor-api
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- include "adc.podSecurityContext" . | nindent 8 }}
      containers:
        - name: supervisor-api
          image: {{ include "adc.image" (dict "root" $ "image" .Values.supervisorApi.image) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            {{- include "adc.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.supervisorApi.port }}
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: {{ .Values.supervisorApi.port | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.supervisorApi.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  labels:
    {{- include "adc.commonLabels" . | nindent 4 }}
    app.kubernetes.io/name: supervisor-api
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: supervisor-api
spec:
  selector:
    app.kubernetes.io/name: supervisor-api
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.supervisorApi.port }}
      targetPort: http
{{- if and .Values.openshift.routes.enabled .Values.supervisorApi.route.enabled }}
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ $name }}
  labels:
    {{- include "adc.commonLabels" . | nindent 4 }}
    app.kubernetes.io/name: supervisor-api
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: supervisor-api
spec:
  to:
    kind: Service
    name: {{ $name }}
  port:
    targetPort: http
  tls:
    termination: {{ .Values.openshift.routes.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.openshift.routes.tls.insecureEdgeTerminationPolicy }}
{{- end }}
{{- end }}
